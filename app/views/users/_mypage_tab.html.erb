<table class="table">
	  <tr><th></th><th>日時</th></tr>
	<% event.each do |event| %>
	  <tr><td><%= link_to event.title, event %></td><td><%= event.start_time.strftime("%Y年%m月%d日 %H:%M") %>から</td></tr>

	<% end %>
</table>
<%= paginate event, remote: true %>