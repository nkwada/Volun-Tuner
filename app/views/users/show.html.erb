<h3>My Page</h3>

<p>ユーザー名: <%= @user.username %></p>

<%= attachment_image_tag @user, :image, :fill, 100, 100, format: 'jpeg' %>

<%= link_to "ユーザー情報編集", edit_user_path(current_user.id) %>

<%= link_to "ボランティアを主催する", new_event_path %>

<p><%= @user.username %>さんがこれまで参加したボランティア活動</p>

<% @user.joined_events.each do |event| %>
  <p>主催者：<%= event.user.username %></p>
  <p>タイトル：<%= event.title %></p>
<% end %>

<p><%= @user.username %>さんがこれまで主催したボランティア活動</p>
	<% @user.events.each do |event| %>
		<%= event.title %>
	<% end %>
